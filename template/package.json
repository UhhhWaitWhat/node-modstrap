#TEMPLATE
{
  "name": "{{name}}",
  "version": "1.0.0",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{repo}}.git"
  },
  "devDependencies": {
    "eslint": "^0.18.0",
    "mocha": "^1.21.4",
    "must": "^0.12.0",
    "version-bump": "^0.1.0",
    "babel": "^4.7.16"
  },
  "scripts": {
    "test": "make test",
    "prepublish": "make test"
  },
  "author": "{{author}}",
  "license": "BSD-3-Clause",
  "eslintConfig": {
    "env": {
      "es6": true,
      "node": true
    },
    "rules": {
      "curly": [2, "multi-line"],
      "quotes": [2, "single"]
    },
    "ecmaFeatures": {
      "modules": true
    }
  }
}
